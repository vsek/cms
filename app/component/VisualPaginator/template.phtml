{*
 * @version    $Id: template.phtml 2 2009-07-13 20:58:52Z david@grudl.com $
 * @param Paginator $paginator
 * @param array     $steps
 *}

{if $paginator->pageCount > 1}
<div class="paginator">
	{if $paginator->isFirst()}
	<span class="button prev"></span>
	{else}
	<a href="{link this, 'page' => $paginator->page - 1}" class="prev"></a>
	{/if}

	{foreach $steps as $step}
	{if $step == $paginator->page}
		<span class="current page">{$step}</span>
	{else}
		<a href="{link this, 'page' => $step}" class="page">{$step}</a>
	{/if}
	{if $iterator->nextValue > $step + 1}<span>…</span>{/if}
	{/foreach}

	{if $paginator->isLast()}
	<span class="button next"></span>
	{else}
	<a href="{link this, 'page' => $paginator->page + 1}" class="next"></a>
	{/if}
</div>
{/if}
